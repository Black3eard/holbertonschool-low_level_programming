#include <stdio.h>
/**
 * main -Entry Point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char decima;

	for (decima = 0; decima <= 9; decima++)
	{
		putchar(decima);
	}
	putchar('\n');
		return (0);
}
