#include <stdio.h>
/**
 * main -Entry Point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int decima;

	for (decima = 48; decima <= 57; decima++)
	{
		putchar(decima);
	}
	putchar('\n');
		return (0);
}
