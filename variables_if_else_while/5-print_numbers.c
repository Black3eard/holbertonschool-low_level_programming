#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int decima;

	for (decima = 0; decima <= 9; decima++)
		printf("%d", decima);
	printf("\n");
	return (0);
}
