#include"main.h"
/**
 * islower - Entry Point
 *
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (c == ([upper])
	{
		_putchar('0');
	}
	if (c != ([upper])
	{
		_putchar('1');
	}
	return (0);
}
